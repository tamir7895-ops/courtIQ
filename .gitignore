# ── IDE / OS ──
.claude/
.DS_Store
Thumbs.db
*.swp
*.swo

# ── Dependencies ──
node_modules/

# ── Environment / Secrets ──
.env
.env.*
!.env.example

# ── Build output (generated by build.js / Capacitor) ──
dist/

# ── Large binary models (use releases or LFS) ──
*.onnx
*.onnx.data
models/
www/models/

# ── Training data & videos (local-only) ──
tools/training-videos/
training/
training_data/
*.mp4
*.mov

# ── Debug / scratch files ──
debug-*.html
Download.mp4
auto-sync.js

# ── Capacitor generated ──
android/app/build/
android/.gradle/
