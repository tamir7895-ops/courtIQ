# CourtIQ — דוח ביקורת אבחון (READ-ONLY)
**תאריך:** 2026-08-05 · **קומיט שנבדק:** `e9fc4d3` (master) · **בילד ב-www/:** `165316e 08-04 11:22`
**מצב עבודה:** אבחון בלבד. לא שונה אף קובץ קוד, לא הורץ build, לא בוצע git. הקובץ היחיד שנכתב הוא הדוח הזה.

**שיטה:** מיפוי ידני של הריפו + השוואת דריפט בין `root/`, `www/` ו-`ios/` + שישה סוכני ביקורת מקבילים (צנרת CV, ביצועי מעטפת, כלכלה/סנכרון, אבטחת לקוח, טיפול בשגיאות/UX, לוגיקת פיצ'רים) + מעבר-פערים שני + **מדידות ריצה אמיתיות בדפדפן** (זמני רינדור, דליפות זיכרון/מאזינים, בדיקת שרידות אוברליי, בדיקת bubbling של אירועים) + בדיקות שרת ישירות מול Supabase (RLS, policies, פונקציות edge, advisors).

---

## 1. תקציר מנהלים — 5 החשודים המובילים לתקיעות ולבאגים

> אלה מדורגים לפי **הסתברות שהם מסבירים את מה שאתה חווה בפועל** ("נתקע לפעמים", "באגים מוזרים", "לא חלק"), לא לפי חומרה תיאורטית.
>
> **תיקון עצמי לאחר המעבר השני:** בגרסה הראשונה של הדוח דירגתי את "החלוניות היתומות" כחשוד מספר 1. המעבר השני הפריך את זה חלקית ואני מתקן: ראו ממצא #1 בטבלה — ההוכחה שלי השתמשה בניווט **תכנותי**, ולמשתמש אמיתי אין דרך להגיע למצב הזה (החלונית ב-z-70 חוסמת את סרגל הניווט ב-z-50, והחלקות מחוברות ל-`#app` שהוא אח של האוברליי). זה נשאר מפגע מבני אמיתי — אבל **לא** ההסבר לתקיעות שלך. הדירוג להלן מתוקן בהתאם.

### חשוד #1 — לולאת הזיהוי יכולה להיתקע לצמיתות באמצע סשן (Worker בלי טיפול בשגיאה)
`features/shot-tracking/shotDetection.js:1994-1997`
ההמרה ל-CHW רצה ב-Web Worker. ה-Promise שעוטף אותו **קורא רק ל-`resolve`** — אין `reject`, אין `_chwWorker.onerror` (אימתתי: אפס תוצאות ל-grep של `onerror` בקובץ), ואין timeout. `_isDetecting=true` נקבע לפני הקריאה ומתאפס **רק** בתוך `.then`/`.catch`. אם ה-Worker קורס או תשובה אובדת — ה-Promise לא נפתר לעולם, `_isDetecting` נשאר `true`, ו-`_scheduleDetection()` לא נקרא שוב: **הזיהוי מת בשקט לשארית הסשן**, בלי שגיאה ובלי הודעה. המצלמה ממשיכה לרוץ, אז נראה שהכול תקין — אבל שום זריקה לא נספרת. אותו מסלול משמש גם את ניתוח הווידאו, שם זה מקפיא את פס ההתקדמות.
בנוסף: אם המכשיר נופל ל-backend `wasm-main-thread` (`shotDetection.js:987`), כל היקש חוסם את ה-UI ל-~1.4 שניות לפי ההערה בקוד עצמו — כל ~200ms. זה freeze מוחשי לכל אורך הסשן.

### חשוד #2 — כשל ברינדור אסינכרוני = מסך לבן שקט, בלי שגיאה ובלי טלמטריה
`app-v10/app.js:119`
הראוטר עוטף ב-try/catch רק את החלק **הסינכרוני** של הרינדור. רוב המסכים מחזירים Promise (home, me, track, social, coach, train, notifications, avatar-customizer, workout-player). השורה `rendered.then(roll, roll)` משתמשת באותה פונקציה גם כהצלחה וגם ככישלון — כלומר **הדחייה נבלעת**: אין באנר שגיאה, אין `console.error`, ואפילו הטלמטריה (`lib/telemetry.js:51` מאזין ל-`unhandledrejection`) לא נדלקת. כל באג רגיל בתוך ה-`.then` (null, שדה חסר) = מסך ריק לגמרי בלי שום עקבות.

### חשוד #3 — צינור התגמולים מנותק: סשן אמיתי לא מזכה ב-XP, לא ברצף ולא בתגים
`app-v10/screens/camera-hud.js:887` · `features/shot-tracking/ShotTrackingScreen.js:2429-2439` · `js/streak.js:209`
1. שמירת הסשן קוראת ל-`saveSessionAtomic(...).catch(function(){})` ולא קוראת ל-XP/streak/badges בכלל.
2. הקוד היחיד שכן מזכה (`saveSessionData` ב-`ShotTrackingScreen.js:2795`) מגיע דרך `renderSummary`, אבל `enterSummaryPhase` עושה `return` מוקדם בכל סשן של app-v10 (`__v10AnalysisOwnsFlow`) — **אימתתי את שרשרת הבקרה בקוד**: הנתיב מת.
3. גם אילו היה חי — `js/streak.js:209` מגדיר `var t = todayStr()` שמאפיל על פונקציית ה-i18n `t()` של אותו מודול, ולכן `t('streak.xp.daily')` בשורה 238 היה זורק `TypeError` לפני שה-XP מוענק. (זו בדיוק המלכודת המוכרת בפרויקט הזה.)
4. **ואפילו ה-XP שכן נצבר (מאתגרים יומיים) לא מוצג למשתמש מחובר:** `lib/data.js:200` עושה `xp = xpData.xp || ud.xp || localXP()` — כלומר הערך מהשרת מנצח. שאילתה לשרת מראה שהחשבון שלך מחזיק `user_data.xp_data.xp = 126` — ערך שנכתב בעידן הדשבורד הישן, ו-app-v10 **לא כותב אליו לעולם** (`js/dashboard.js`, הכותב היחיד, לא נטען). התוצאה: מספר ה-XP במסך הבית קפוא, בזמן שהמטבעות (שנגזרים מ-XP מקומי) כן זזים. שני מקורות אמת סותרים.

### חשוד #4 — האתחול תלוי ב-CDN חיצוני חוסם (ובלי גיבוי מקומי)
`app-v10/index.html:117`
`supabase-js` נטען מ-`cdn.jsdelivr.net` כסקריפט **סינכרוני, ראשון ברשימה, בלי `defer`, בלי fallback מקומי ובלי SRI**. ‏ORT ו-MediaPipe דווקא כן עברו vendoring מקומי בדיוק מהסיבה הזאת (ההערות בקוד מסבירות: "מגרש בלי קליטה"). אימתתי בזמן ריצה שהבקשה יוצאת בכל טעינה. ‏`DOMContentLoaded` (ואיתו `app.bootstrap()`) מחכה לסקריפט הזה — כלומר במגרש עם קליטה גרועה או DNS איטי, המשתמש רואה את מסך הפתיחה מסתובב עד ל-timeout של ה-WebView (עשרות שניות). זה "האפליקציה נתקעה בהתחלה".

---

## 2. טבלת ממצאים מדורגת

חומרה: **C**=Critical · **H**=High · **M**=Medium · **L**=Low
ודאות: **ודאי** = אומת ישירות בקוד/בריצה · **סביר** = המנגנון ודאי, התדירות/ההשפעה מוערכת · **לא ודאי** = השערה שדורשת אימות

| # | כותרת | חומרה | קטגוריה | נתיב:שורה | מה קורה | איך לשחזר | השפעה | תיקון מוצע (לא בוצע) | ודאות |
|---|---|---|---|---|---|---|---|---|---|
| 1 | חלוניות body-level שורדות ניווט (מפגע מבני — **לא** נגיש למשתמש) | **M**<br>*(הורד מ-C)* | תקיעה/UI | `me.js:329`, `plan.js:281`, `landing.js:184`, `camera-hud.js:978` | הראוטר (`app.js:76`) מנקה רק את `#app`; אוברליים ב-`document.body` שורדים ניווט עם `position:fixed; inset:0; z-index:70; pointer-events:auto` — **אימתתי בריצה** | **רק ניווט תכנותי.** למשתמש אין מסלול: החלונית (z-70) מכסה את סרגל הניווט (z-50), וההחלקות מחוברות ל-`#app` שהוא **אח** של האוברליי ולא אב שלו — כלומר מגע על האוברליי לא מגיע ל-handler | היום: אפס. מחר: כל קוד שיקרא ל-`ctx.go()` בזמן שחלונית פתוחה (למשל טיפול ב-deep-link או אירוע auth) יוצר מסך תקוע | מנקה גלובלי ב-`go()` שמסיר אוברליים body-level — הגנה לעתיד | **ודאי** (המנגנון, נמדד) / **נשלל** (נגישות למשתמש) |
| 2 | Worker בלי `onerror`/timeout → מוות שקט של הזיהוי | **C** | תקיעה/CV | `shotDetection.js:1994-1997`, `yoloxWorker.js` | ה-Promise רק `resolve`; אין `reject`, אין `onerror`, אין timeout; `_isDetecting` לא מתאפס | קריסת Worker / הודעה אבודה (סביר במכשירים חלשים) | הסשן ממשיך "לרוץ" אך לא סופר כלום; בניתוח וידאו — פס התקדמות תקוע לנצח | לעטוף ב-`Promise.race` עם timeout 2-3 שניות + `onerror` שדוחה + נפילה חזרה למסלול main-thread | **ודאי** (הפער בקוד) / **סביר** (תדירות הקריסה) |
| 3 | דחיית Promise ברינדור נבלעת → מסך לבן בלי שגיאה | **C** | שגיאות | `app-v10/app.js:119` | `rendered.then(roll, roll)` — אותו handler להצלחה ולכישלון | באג כלשהו בתוך `.then` של מסך אסינכרוני | מסך ריק, אין הודעה, אין `client_errors` | `rendered.then(roll, function(e){ roll(); /* להציג באנר */ throw e; })` | **ודאי** |
| 4 | סשן אמיתי לא מזכה ב-XP/רצף/תגים | **C** | כלכלה | `camera-hud.js:887`; נתיב מת: `ShotTrackingScreen.js:2429-2439`→`2854` | `enterSummaryPhase` חוזר מוקדם ב-app-v10, ולכן `saveSessionData` (המזכה היחיד) לא רץ | לסיים סשן מלא ולהשוות `localStorage['courtiq-xp']` לפני/אחרי | כל מערכת ההתקדמות מנותקת מהמשחק בפועל | לקרוא ל-`XPSystem.grantXP` + `StreakSystem.checkIn` + badges אחרי `saveSessionAtomic` מוצלח | **ודאי** — שרשרת הבקרה נבדקה ידנית |
| 5 | `var t` מאפיל על `t()` — הענקת ה-XP היומי זורקת | **C** | באג/i18n | `js/streak.js:209` (שימוש: `:238`, `:247`) | משתנה מקומי `t` (מחרוזת תאריך) מאפיל על פונקציית ה-i18n של המודול | לקרוא ל-`StreakSystem.checkIn()` | גם אחרי תיקון #4, ‏XP יומי ומיילסטונים ייכשלו בשקט | לשנות שם ל-`todayD` | **ודאי** |
| 6 | שמירת סשן נכשלת בשקט — אין תור אופליין למשתמש מחובר | **C** | אובדן נתונים | `camera-hud.js:888`; `shotService.js:182-222` | `.catch(function(){})`; חיץ ה-localStorage קיים רק לאנונימיים | להתנתק מהרשת בדיוק בסוף סשן | הסשן אבד לצמיתות; המסך מציג סיכום כאילו נשמר | לחסן לאותו תור אופליין + טוסט "יסונכרן מאוחר יותר" | **ודאי** |
| 7 | `claude-proxy` — מעבר חופשי ל-Anthropic על חשבון המפתח שלך | **C** | אבטחה/עלות | `supabase/functions/claude-proxy/index.ts:46-56` (פרוס, גרסה 5, ACTIVE) | מעביר את גוף הבקשה כמו שהוא: כל מודל, כל `max_tokens`, בלי מכסה | כל משתמש רשום → `curl` לפונקציה עם ה-JWT שלו | חיוב בלתי מוגבל; שימוש כ-LLM חינמי; מיצוי rate-limit של המפתח | להסיר את הפונקציה (הקוד עצמו מציין שהיא רק ל"legacy") או להצמיד מודל/תקרה/מכסה כמו ב-`coach-chat` | **ודאי** — קראתי את הפונקציה הפרוסה |
| 8 | ‏`v10:auth` נשלח על `document` אך מואזן על `window` → סנכרון ראשון לא רץ | **H** | סנכרון | `auth-state.js:22` מול `lib/sync.js:266` | `CustomEvent` ללא `bubbles:true` לא מגיע ל-window. **הוכחתי בדפדפן**: `reachesWindow_default=false` | להתחבר ולצפות מתי המשיכה הראשונה קורית | אחרי התחברות ההתקדמות מהענן מופיעה רק אחרי עד 60 שניות (טיימר) | להוסיף `bubbles:true`, או להאזין על `document` | **ודאי** — אומת אמפירית |
| 9 | תוכנית האימונים לא מסונכרנת בכלל (אי-התאמת מפתח) | **H** | סנכרון/נתונים | `lib/plan.js:20` (`courtiq_plan_v2`) מול `lib/sync.js:114,199` (`courtiq-training-plan-v1`) | הסנכרון דוחף/מושך מפתח שאף אחד באפליקציה לא קורא ממנו | לבנות תוכנית, להתחבר במכשיר אחר | התוכנית נשארת מקומית לנצח; ומעבר לכך — `apply()` כותב מפתח מת | ליישר את `sync.js` ל-`courtiq_plan_v2` | **ודאי** — שני המפתחות אומתו ב-grep |
| 10 | ‏XP מוצג מהשרת אך נכתב רק מקומית → מספר קפוא | **H** | כלכלה | `lib/data.js:200`; אין כותב ל-`user_data.xp_data` באפליקציה הנטענת | `xp = xpData.xp || ud.xp || localXP()` — השרת מנצח; השרת מחזיק ערך מעידן הדשבורד (במסד: `xp_data.xp=126` לחשבון שלך) | להרוויח XP מאתגר יומי ולהסתכל במסך הבית | ה-XP "לא עולה"; מטבעות ו-XP מספרים סיפורים שונים | לכתוב XP לשרת, או להעדיף `localXP()` כמו שנעשה ב-streak/avatar | **ודאי** — כולל אימות מול המסד |
| 11 | `courtiq:session-saved` — שלושה מאזינים, אפס משגרים | **H** | נתונים ישנים | `lib/sync.js:257`, `lib/notify.js:164`, `lib/data.js:632` | האירוע לא נשלח משום מקום (grep) | לסיים סשן ולעבור למסך הבית | memo של 30 שניות לא מתבטל → נתונים ישנים; סנכרון מתעכב ל-60 שניות | לשגר את האירוע אחרי שמירה מוצלחת (כמו `courtiq:plan-saved`) | **ודאי** |
| 12 | דגל `courtiq_me_return` תקוע מחזיר להגדרות במקום לפרופיל | **H** | ניווט | `me.js:355-358` (קביעה), `me.js:716` (צריכה); ניקוי רק ב-`nav.js:66`, `mobile.js:133` | כל `ctx.go()` פנימי לא מנקה את הדגל | הגדרות → התראות → "התחל אימון" → סוף סשן → בית → לחיצה על האווטאר → נפתחות **הגדרות** | ניווט מוזר שמופיע דקות אחר כך בלי סיבה נראית | לנקות את הדגל מרכזית ב-`go()`/`back()`, או TTL קצר | **ודאי** — שרשרת נבדקה |
| 13 | ‏`screens/auth.js` לא מיישב את `courtiq_onboarded` | **H** | אימות/אונבורדינג | `auth.js:88-100` מול `landing.js:135-139`; `auth-state.js:71-90` מוחק את הדגל בהתנתקות | התחברות דרך ההגדרות לא בודקת אם המשתמש כבר עבר אונבורדינג | להתנתק → להתחבר מההגדרות → לסגור ולפתוח את האפליקציה | משתמש ותיק נזרק שוב לאונבורדינג מלא; או להפך — נרשם חדש מדלג עליו | לשקף את הבדיקה של `landing.js`, או לרכז את ההחלטה במקום אחד | **ודאי** |
| 14 | ‏backend `wasm-main-thread` חוסם את ה-UI ~1.4 שניות להיקש | **H** | ביצועים/CV | `shotDetection.js:984-988`, ההערה בקוד ב-`:1023` | מפל ה-backends נופל למסלול סינכרוני על ה-main thread | מכשיר בלי WebGPU שבו גם `wasm-proxy` נכשל | קיפאון UI חוזר לכל אורך הסשן | לא להריץ בקצב הזה על backend הזה (או לסרב לו ולהציג באנר) | **סביר** — המנגנון ודאי, השכיחות לא נמדדה |
| 15 | דליפת מאזין `resize` בכל כניסה למצלמה | **H** | דליפה | `ShotTrackingScreen.js:313-317` (רישום ב-`bindEvents` שנקרא בכל `openScreen`), אין `removeEventListener` | מאזין חדש על `window` בכל סשן | לפתוח/לסגור סשן N פעמים ואז לסובב מכשיר | `resizeCanvas()` רץ N פעמים על אותו אירוע — עבודה מיותרת ברגע כבד ממילא | לרשום פעם אחת בטעינת המודול או להסיר ב-`closeScreen` | **ודאי** |
| 16 | דליפת מאזין `keydown` + DOM בסגירת רפליי בכפתור X | **H** | דליפה | `js/video-review.js:373-374` מול `:240`, `:377-386` | ההסרה קורית רק במסלול Escape | לפתוח ולסגור רפליי ב-X כמה פעמים | מאזינים ותת-עצי DOM נשמרים לכל אורך חיי האפליקציה | להסיר את המאזין בתוך `closeReplay()` | **ודאי** |
| 17 | האוברליי של ניתוח הווידאו לא מפורק בניווט | **H** | תקיעה | `camera-hud.js:978` (הוספה ל-body), `:1337` (`leaveObs` יוצא מוקדם כש-`__v10AnalysisOwnsFlow`) | בזמן ניתוח, מנגנון הניקוי מנוטרל בכוונה | ניווט/back חומרתי באנדרואיד באמצע ניתוח | אוברליי מסך-מלא נתקע מעל האפליקציה | לנקות את האוברליי בסיום/ביטול גם כשהדגל דלוק | **ודאי** (המנגנון) / **לא ודאי** (האם ה-trigger נגיש בפועל — הניווט מוסתר בזמן ניתוח) |
| 18 | ‏`supabase-js` מ-CDN: חוסם, בלי fallback, בלי SRI | **H** | אתחול/אבטחה | `app-v10/index.html:117` | סקריפט סינכרוני ראשון; ORT/MediaPipe כן מקומיים | להפעיל בלי רשת / עם DNS איטי | מסך פתיחה תקוע עד timeout; ובנוסף — נקודת אמון יחידה בלי integrity hash | לעשות vendoring כמו ל-ORT, או לפחות `integrity=` + `defer` + fallback מקומי | **ודאי** — אומת גם בזמן ריצה |
| 41 | **הכפתור "חזור" של אנדרואיד לא מחובר למודל הניווט** | **H** | ניווט/Capacitor | אין `@capacitor/app` ב-`package.json`; אין `backButton` listener (grep=0); `MainActivity.java` ריק; `app.js:72` משתמש רק ב-`replaceState` | מודל ה-back-trail שבניתי ב-`app.js` **לא נגיש** מהכפתור החומרתי; ו-`replaceState` לא מגדיל היסטוריית WebView, אז גם ה-fallback הנייטיב חסר לאן לחזור | לחיצה על "חזור" באנדרואיד מכל עומק | האפליקציה ממזערת/יוצאת במקום לחזור מסך — התנהגות שונה לגמרי מכל שאר כפתורי החזרה | להוסיף `@capacitor/app` ולחבר `backButton` ל-`window.app.back()` | **סביר** — היעדר החיווט ודאי; התנהגות ה-fallback המדויקת לא אומתה |
| 42 | **אין CI לאנדרואיד; התיקייה המקומית מיושנת קשות** | **H** | בילד/CI | `.github/workflows/` = pages בלבד; `codemagic.yaml` = iOS בלבד; `android/.gitignore:96` | `android/app/src/main/assets/public/app-v10/` מכיל ~30 קבצים — בלי `lib/i18n.js`, בלי `lang/`, בלי `lib/v12.js`, בלי `screens/landing|shop|plan|social` | `npm run open` **לא** בונה קודם | בילד אנדרואיד מהמצב הנוכחי היה עולה עם `V12I18n` לא מוגדר וקישורים מתים | להוסיף מסלול CI לאנדרואיד, או להצהיר שהוא רדום | **סביר** (הפער ודאי; האם אי פעם נשלח כך — לא ניתן לאימות) |
| 43 | בחירת שפה מההגדרות מאבדת את המיקום אחרי ה-reload | **M** | i18n/UX | `me.js:314-319` מול `me.js:355,716` | מסלול הבורר לא מדליק את `courtiq_me_return` לפני `V12I18n.set()` שעושה reload | הגדרות → שפה → לבחור es/ar/ru/fr/pt | חוזרים לדף הפרופיל הראשי במקום להגדרות | להדליק את הדגל לפני `set()` (כמו ב-`goFromSettings`) | **ודאי** |
| 44 | 27 קבצי `js/` מתים נשלחים בבאנדל | **L** | ניפוח | `js/` מכיל 41 קבצים; רק 14 נטענים; מתוכם `daily-workout.js` ו-`shot-analysis.js` לא נצרכים בכלל | ה-`DROP` ב-`ios-sync.js` לא מסנן אותם | — | בייטים ו-parse מיותרים בכל אתחול, גם באייפון | למחוק/להסיר תגיות | **ודאי** |
| 45 | `<input type=file>` נשאר ב-DOM בביטול בחירה | **L** | דליפה | `lib/data.js:504-524` | ההסרה רק בתוך מאזין `change`; ביטול לא מפעיל אותו | לפתוח בוחר קבצים ולבטל | הצטברות בלתי נראית (הקוד עצמו כרגע לא נקרא מאף מסך) | להאזין גם ל-`cancel` | **סביר** |
| 19 | טיימר האימון מבוסס טיקים ולא חותמות זמן | **M** | נכונות | `workout-player.js:211-215` | `state.seconds -= 1` בכל `setInterval` | לנעול מסך באמצע תרגיל ולחזור | הטיימר "עוצר" ברקע ב-iOS ומציג זמן שגוי | לחשב מ-`Date.now()` בכל טיק | **ודאי** (המנגנון) / **לא ודאי** (איזו התנהגות רצויה מוצרית) |
| 20 | מטמון הפנים מפרסר את כל ה-JSON בכל תמונה | **M** | ביצועים | `lib/v12.js:341-373`; נקראים ב-`coach.js:317,510,797,833` | `JSON.parse` של כל המטמון (עד 24 תמונות base64) לכל קריאה | לפתוח שיחת מאמן ארוכה | עשרות ms חסימה לכל בועה; במדידה שלי המטמון הוא 26.9KB — יגדל עם השימוש | לפרסר פעם אחת לזיכרון ולסנכרן ב-`set` בלבד | **ודאי** |
| 21 | `A.load()`/`isOwned()` נקראים בתוך לולאות רינדור | **M** | ביצועים | `shop.js:151-216`, `avatar-customizer.js:79-129`; מימוש: `avatar.js:300-316`, `:393-407` | לכל אחד מ-~60 פריטים: `getItem`+`JSON.parse`+סריקה לינארית של כל הקטלוג | לפתוח את החנות; כל קנייה מפעילה `paint()` מחדש | ג'אנק קצר בפתיחה ובכל לחיצה | להרים את הקריאות מחוץ ללולאה (Set של ownedIds) | **ודאי** |
| 22 | `t()` קורא ל-localStorage בכל קריאה | **M** | ביצועים | `lib/i18n.js:100-103, 112-121` | `current()` → `localStorage.getItem` בכל תרגום | רינדור ספריית התרגילים (178 כרטיסים) | מאות קריאות I/O סינכרוניות מיותרות (מרוכך ע"י ה-batching הקיים) | לזכור את השפה עד `set()`/אירוע החלפה | **ודאי** (המנגנון) / **סביר** (השפעה מתונה) |
| 23 | `document.write` בטעינת חבילות שפה חוסם את הפרסור | **M** | אתחול | `app-v10/index.html:127-131, 173-186` | ‏parser blocking מוצהר; קורה בכל טעינה למשתמש לא-אנגלי | לפתוח את האפליקציה בעברית | דוחה את ה-TTI; 80 סקריפטים סינכרוניים בלי `defer` | להזריק `<script>` דרך `appendChild` (כמו שכבר נעשה ל-studio/smoke) | **ודאי** (המנגנון) / **סביר** (גודל ההשפעה במכשיר) |
| 24 | סקירת פרופיל לא נשמרת לשרת (`updateProfile` קוד מת) | **M** | נתונים | `js/data-service.js:18` — הקוראים היחידים הם `js/onboarding.js` ו-`js/avatar-customizer.js`, ששניהם **לא נטענים** | אונבורדינג ו"עריכת פרופיל" כותבים ל-localStorage בלבד | לערוך פרופיל ולהיכנס ממכשיר אחר | שם/עמדה/גובה לא עוברים בין מכשירים. **תיקון לדוח סוכן:** בפועל `position` כן מוצג נכון למשתמשים ותיקים (במסד: `position='pg'`) כי הערך נכתב בעידן הישן; הבעיה תפגע בעיקר בחשבונות **חדשים** | לקרוא ל-`updateProfile` מהמסלולים החיים | **ודאי** (הקוד מת) / **מתוקן** לגבי התסמין |
| 25 | ‏`__courtiqDebug` דלוק כברירת מחדל בבילד ייצור | **M** | ייצור/UX | `app-v10/index.html:110-113`; צרכנים: `camera-hud.js:246, 900, 1194` | כבוי רק עם `?debug=0` או localStorage | לפתוח מצלמה בבילד TestFlight | מדדי פיתוח (backend/timing) גלויים למשתמשי קצה | להפוך את ברירת המחדל ל-off ולהדליק בפירוש בפיתוח | **ודאי** |
| 26 | היסטוריית שיחה של המאמן יכולה לזלוג בין דמויות | **M** | נתונים | `lib/coach-ai.js:66-76, 349-419`; `coach.js:292-297` | `history` הוא משתנה מודול שמוחלף ב-`setPersona`; ה-callback לא מצלם snapshot | לשאול את Scout ולעבור ל-Splash לפני שהתשובה חוזרת | התשובה נשמרת ל-persona הלא נכון; השאלה המקורית אובדת | לצלם persona/מערך ב-`ask()` ולוודא לפני הכתיבה | **סביר** — חלון תזמון צר |
| 27 | מרוץ סנכרון יכול "להחזיר" הוצאה של קנייה | **M** | כלכלה | `lib/sync.js:186` | `shop.spent` נדרס בערך סקלרי מהשרת בזמן שקנייה מקומית כבר נרשמה | קנייה בדיוק בזמן מחזור סנכרון | מטבעות מנופחים זמנית עד המחזור הבא | לגזור `spent` מ-`owned` מקומית, או רק להעלות ולא להוריד | **סביר** — מתקן את עצמו תוך 60 שניות |
| 28 | היקש ישן יכול לדלוף לסשן חדש | **M** | מרוץ/CV | `shotDetection.js:1138-1169` (`stop()` לא מאפס `_isDetecting`), `:1055-1136` | מנוע singleton בלי טוקן סשן | לצאת ולהיכנס למצלמה מהר בזמן היקש איטי | זיהוי זר נכנס ל-tracker של הסשן החדש; רגע של טיימר כפול | טוקן מונוטוני לכל `start()` | **סביר** |
| 29 | שני `MutationObserver` חופפים; אחד לא בודק את הדגל | **M** | UI/כפילות | `camera-hud.js:1317-1333` מול `lib/data.js:454-477` | האחד קורא ל-`setNav(true)` בלי לבדוק `__v10AnalysisOwnsFlow` | סיום סשן רגיל | הבזק אפשרי של סרגל הניווט לפני מסך הניתוח | למחוק את הכפול או ליישר את הבדיקה | **ודאי** (הא-סימטריה) / **סביר** (הנראות בפועל) |
| 30 | ‏`?studio` פותח את עורך העיצוב בכל אתח (OR במקום AND) | **L** | ייצור | `app-v10/index.html:298-300` | הבדיקה מחוברת ב-`||` לבדיקת ה-hostname | לפתוח את גרסת הווב עם `?studio` | פאנל dev נגיש לכל מבקר (ללא גישה לנתונים) | להצמיד ל-`&&` עם בדיקת hostname | **ודאי** |
| 31 | דפי דיבאג ו-54MB `_audit` נשלחים ל-www | **L** | ייצור/גודל | `build.js:25-37` מעתיק את השורש; `www/_audit` 54MB, `www/_eval` 16MB, `www/debug-*.html` | אין רשימת החרגה | לפתוח `<site>/debug-eval-run.html` | דלף פרטי כיול פנימיים; ניפוח פריסת הווב | להחריג בבילד ייצור | **ודאי** |
| 32 | `tfhub.dev` ב-allowNavigation ולא בשימוש | **L** | אבטחה | `capacitor.config.json:14` | אין אף התייחסות בקוד (grep) | — | משטח תקיפה מיותר | להסיר | **ודאי** |
| 33 | `coach_usage` עם RLS דלוק ובלי policies | **L** | אבטחה/DB | Supabase advisor + `pg_policies` (0 policies) | הטבלה נגישה רק ל-service role (הפונקציה `coach_touch` היא SECURITY DEFINER) | — | תקין בפועל, אבל מסמן "כוונה לא מוצהרת" | להוסיף policy מפורשת או להעביר לסכימה פרטית | **ודאי** — נבדק בשאילתה |
| 34 | הגנת סיסמאות דלופות כבויה ב-Auth | **L** | אבטחה | Supabase advisor | HaveIBeenPwned לא מופעל | — | משתמשים יכולים לבחור סיסמאות שדלפו | להפעיל בלוח הבקרה | **ודאי** |
| 35 | כפתורי אייקון בלי `aria-label` | **L** | נגישות | `social.js:232,234`; `plan.js:219,295,297,357,359`; `me.js:305` | חריגים לדפוס שכן מיושם ברוב האפליקציה | קורא מסך | "button" בלי שם | להוסיף תוויות | **ודאי** |
| 36 | יעדי מגע קטנים מ-44px | **L** | UX | `v12.css:777` (`.c12-back` 40×40), `v11.css:176` (36×36), `v12.css:1107,2191` (36×36) | מתחת להנחיית HIG | — | דיוק לחיצה; לא חוסם פעולה קריטית | להגדיל ל-44 | **סביר** |
| 37 | מחיקת חשבון בלי טיפול בשגיאה או מצב טעינה | **L** | UX/שגיאות | `me.js:421-428`; `auth-state.js:98-113` זורק | אין `.catch`, אין disable | לגרום לכשל בפונקציית ה-edge | לחיצה שלא עושה כלום, בלי משוב, בפעולה הרסנית | להוסיף `.catch` + מצב busy | **ודאי** |
| 38 | ניתוח וידאו בלי כפתור ביטול | **L** | UX | `camera-hud.js:893-1235` (אין affordance), `offlineProcessor.js:312` (תומך `signal`) | המנגנון קיים אך לא מחובר ל-UI | ניתוח ארוך במכשיר איטי | אי אפשר לצאת אלא בהמתנה | לחבר `AbortController` לכפתור | **ודאי** |
| 39 | לולאת reload אפשרית אם חבילת שפה חסרה | **L** | i18n | `lib/i18n.js:150-176`; טוען: `index.html:173-186` | `set()` כותב את השפה ואז בודק sentinel; אם החבילה 404 — הבדיקה תיכשל שוב | פריסה חלקית של `www/` | כל הקשה על השפה = reload מלא, בלי הודעת שגיאה | לגבות עם fallback ל-`en` אחרי ניסיון כושל | **לא ודאי** — לא הצלחתי לשחזר; המפתחות אומתו קיימים בכל החבילות |
| 40 | לינק עמוק עוקף את שער האונבורדינג | **L** | ניווט | `app.js:130-137` | הבדיקה חלה רק על `#home`/hash ריק | לפתוח `#track` בהתקנה חדשה | מסך פנימי בלי פרופיל/תוכנית | לבדוק את הדגל לכל יעד לא-TRANSIENT | **לא ודאי** — מסלול נדיר |

---

## 3. בדקתי — ותקין

**זה החלק שחשוב לא פחות: המון היפותזות סבירות נבדקו ונפסלו בראיות.**

### נבדק בזמן ריצה (מדידות שלי, לא ניתוח סטטי)
* **אין דליפת זיכרון בניווט.** הרצתי 6 סבבים × 8 מסכים (48 ניווטים): הזיכרון נשאר **5.6-6.0MB** (שטוח), ומספר צמתי ה-DOM נשאר **349** בדיוק. אלמנטים ומאזיניהם משתחררים כראוי.
* **אין הצטברות מאזינים ברמת window/document בניווט.** מדדתי עם patch על `addEventListener`: לאורך 10 מסכים ו-48 ניווטים — **0** מאזיני window ו-**0** מאזיני document חדשים. ההיפותזה המרכזית של "דליפת מאזינים בראוטר" **נפסלה**.
* **אין `setInterval` שנשאר תלוי.** לאורך כל הריצה: 0 אינטרוולים חדשים; כל הטיימרים שנמצאו בקוד מזוהים עם מסלול ניקוי (MutationObserver / `isConnected` / `stopPolling`).
* **זמני רינדור סבירים.** הכבד ביותר — ספריית התרגילים — 1,543 צמתים ו-60 SVG, מתייצב תוך ~1 שנייה עם batching מפורש של 30 כרטיסים לפריים. שאר המסכים 23-335 צמתים.
* **‏`localStorage` קטן.** סה"כ 29KB, 4 מפתחות; הגדול הוא מטמון הפנים (26.9KB) — רחוק מהמכסה.

### נבדק בקוד ונמצא תקין
* **RLS ב-Supabase — תקין ומלא.** כל 20 הטבלאות עם RLS דלוק; בדקתי כל policy אחת-אחת: כולן `auth.uid() = user_id` (או requester/addressee ב-friendships). אין policy מתירנית. `save_ai_session_atomic`, `add_friend_by_code`, `friend_leaderboard` — כולן גוזרות את הזהות מ-`auth.uid()` ולא מקלט הלקוח.
* **אין סודות בקוד הלקוח.** רק המפתח הפומבי (`sb_publishable_...`) ב-`js/env.js`. אין קריאה ישירה מהלקוח ל-`api.anthropic.com`.
* **אין XSS במסלולים החיים.** צ'אט המאמן מרונדר דרך `h({text:...})` = `textContent`. כל שימושי `innerHTML` באפליקציה החיה מזריקים מספרים או enum פנימיים בלבד.
* **ניתוב ה-hash מאומת מול רשימת מסכים** (`app.js:60`), אין `eval`/`new Function`.
* **משמעת record-XOR-infer שרדה.** התיקון מ-2026-07-21 (`_recordingIdle`) שלם ולא נסוג.
* **‏ONNX session היא singleton** עם dedupe לטעינות מקבילות; אין יצירה חוזרת בניווט. טנזורים נזרקים גם ב-`then` וגם ב-`catch`.
* **‏MediaStream נסגר כראוי** (`stopCamera` עוצר כל track, מנקה `srcObject`, משחרר blob URL).
* **‏PoseDetector הוא singleton אמיתי.**
* **‏Watchdog למצלמה מתה** בודק פיקסלים אמיתיים ומרים את הזרם מחדש — עיצוב טוב.
* **‏`offlineProcessor` נמנע מ-setTimeout לצעידת פריימים** (בגלל clamping ברקע) ומחזיק watchdog של 25 שניות.
* **תיקון ה-taint ב-iOS** (blob → FileReader) קיים ונכון.
* **‏`getSessions` ממומש עם memo של 30 שניות** שמשותף לכל הקוראים.
* **ניקוי בהתנתקות עובד** ובנוי כ-KEEP-list (ברירת המחדל הבטוחה: מפתח חדש נמחק) — פותר את דליפת החשבונות במכשיר משותף.
* **אין התנגשות שמות תרגילים** (סרקתי את כל ~190 הרשומות).
* **מוסכמת `dow` 0=שני עקבית** בין `plan.js`, `coach-ai.js` והפרומפט בשרת.
* **תביעת אתגר יומי אידמפוטנטית** (כתיבה סינכרונית לפני ההענקה).
* **קנייה בחנות אטומית** (קריאה-בדיקה-כתיבה סינכרונית, אין חלון ל-double-spend).
* **פנקס ההקפאות (freeze) בנוי כשתי קבוצות append-only** — עמיד לשידור חוזר.
* **`prefers-reduced-motion` מכובד** ב-10 קבצים.
* **`build.js` ← `git ls-files` מאשר** שכל מה שה-CI צריך נמצא ב-git: המודל `basketball_yolox_tiny_m6_fp16.onnx`, ‏`vendor/ort/*`, ‏`vendor/mediapipe/*` (כולל `pose_landmarker_full.task` 9.4MB).
* **`KEEP_MODELS` ב-`ios-sync.js:29` תואם בדיוק** ל-`modelPath` ב-`shotDetection.js:967-968` (`basketball_yolox_tiny_m6_fp16.onnx`). זו מלכודת מוכרת — כרגע היא **סגורה**.

---

## 4. דריפט `root/` מול `www/` מול `ios/`

בדקתי ע"י השוואת hash (MD5) קובץ-אחר-קובץ.

### `root/` → `www/` — **נקי**
* **קובץ אחד שונה:** `app-v10/index.html` — וזה **מכוון**: `build.js:73` מחליף `?v=dev` בהאש של הקומיט. לא באג.
* **0 קבצים חסרים ב-www.**
* **2 קבצים יתומים ב-www בלבד:** `www/app-v10/styles/home-v11.css`, `www/styles/courtiq-ui/components/court-svg.css` — שרידים מבנייה ישנה שהמחיקה במקור לא ניקתה. ‏`build.js` מעתיק ולא מסנכרן (אין מחיקה), ולכן שאריות נשארות לנצח. **המלצה:** לנקות את `www/` לפני בנייה, או להוסיף שלב מחיקה.
* **המודלים זהים** בין `root/models` ל-`www/models` (10 קבצים, אותם גדלים).

### `www/` → `ios/App/App/public` — **מיושן מקומית, אך לא רלוונטי לבילד**
* 37 קבצים שונים, 50 קבצים חסרים לגמרי (כל חבילות השפה, `lib/i18n.js`, `lib/sync.js`, `lib/avatar.js`, `screens/landing.js` ועוד) — כלומר העותק המקומי ב-iOS הוא מלפני עידן ה-i18n.
* **זה לא מסביר באגים בטלפון:** ‏`codemagic.yaml` מריץ `npm run ios:sync` → `node build.js` + `npx cap sync ios`, כלומר ה-CI מייצר את התיקייה מחדש בכל בילד. העותק המקומי הוא זבל בלבד.
* **ראוי לניקוי** כדי שלא יטעה בבדיקות ידניות עתידיות (`ios/App/App/public/models` מכיל מודל **אחר** לגמרי — `v7b1_fp16` — בעוד שהקוד טוען `m6_fp16`; בילד לוקאלי מ-Xcode היום היה נופל ל-color-only).
* `www/` אינו ב-git (מאומת: `git ls-files www` = 0), כמצופה.

---

## 5. מה לא הספקתי / דורש בדיקה ידנית ממך

0. **הערה על שיטת העבודה — מה שהמעבר השני לימד.** הממצא שדירגתי ראשון (חלוניות יתומות) הוכח מבחינת המנגנון אבל **נשלל** מבחינת נגישות למשתמש, אחרי שהמעבר השני הצביע על כך שההוכחה שלי השתמשה בניווט תכנותי. תיקנתי את הדירוג ואת החומרה. אני מציין את זה במפורש כדי שתדע: כל ממצא שמסומן "ודאי" נבדק בקוד או בריצה, אבל **"ודאי שהמנגנון קיים" ≠ "ודאי שזה מה שקורה לך"** — ולכן סעיף 5 כאן קיים.
1. **מדידה על מכשיר אמיתי.** כל מדידות הביצועים שלי הן בכרום על דסקטופ. הקיפאונים החשודים (#2, #14) תלויים ב-backend שנבחר בפועל ב-iPhone שלך. **בקשה:** פתח סשן מצלמה עם `?debug=1`, צלם את תג ה-backend (`webgpu` / `wasm-proxy` / `wasm-main-thread`) ואת ה-ms להיקש — זה מכריע בין "לא רלוונטי" ל-"זה הבאג".
2. **שחזור התקיעה שאתה חווה.** כשזה קורה — האם הסרגל התחתון מגיב? האם הנגיעה הראשונה "נבלעת" ואז הכול עובד (זה ממצא #1), או שהמסך מת לגמרי (זה #2/#3)? זו הבחנה שתחתוך את רשימת החשודים בחצי.
3. **האם `claude-proxy` עדיין נחוצה?** לא מצאתי קורא חי באפליקציה, אבל לא אמחק בלי אישורך. אם היא מיותרת — זו הסרה של סיכון עלות בלתי מוגבל בפעולה אחת.
4. **בדיקת מגמת חיוב ב-Anthropic.** אם היה שימוש לרעה ב-`claude-proxy`, זה ייראה שם. לא ניגשתי לחשבונות חיוב.
5. **אונבורדינג מלא מקצה לקצה** (חשבון חדש לגמרי) — לא הרצתי; ממצאים #13 ו-#24 נוגעים בדיוק שם.
6. **התנהגות ברקע ב-iOS** (טיימר האימון, השהיית טיימרים, שחזור מצב) — דורש מכשיר.
7. **‏`nbjw.html` בשורש הריפו** (306KB, לא ב-git, מאמר מחקרי על רישום מגרשי ספורט) — נראה כמו קובץ עבודה שנשכח. לא נגעתי; שווה למחוק ידנית.
8. **ניקוי `www/` והעותק המיושן ב-`ios/`** — פעולות מחיקה, לא ביצעתי.
9. **בילד אנדרואיד** — ממצא #42: אין CI, והתיקייה המקומית מיושנת מאוד. אם אנדרואיד רדום כרגע, שווה להצהיר על זה כדי שאף אחד לא יבנה ממנה בטעות.
10. **מעבר שני הושלם** ושולב בדוח (ממצאים 41-45 + התיקון לממצא #1).

---

## נספח — היקף שנבדק

**מיפוי:** 70 סקריפטים ב-`app-v10/index.html`; 18 מסכים; 23 ספריות ב-`app-v10/lib`; 9 קבצי `features/shot-tracking`; 14 קבצי `js/` שנטענים בפועל (מתוך ~30 בתיקייה).
**נפחים:** `www/` 346MB (מתוכם `models` 150MB, `_audit` 54MB, `vendor` 41MB, `_eval` 16MB); בילד ה-iPhone אחרי הגיזום ~55MB (‏`vendor` 40MB — בעיקר `ort-wasm...jsep.wasm` 21MB ו-`pose_landmarker_full.task` 9.4MB).
**שרת:** 20 טבלאות, 59 policies, 4 פונקציות edge, 5 התראות advisor.
**כלים:** השוואת hash תלת-כיוונית, מדידות `performance.memory` / `performance.getEntriesByType`, patching של `addEventListener`/`setInterval` לספירת דליפות, בדיקת bubbling אמפירית, שאילתות SQL קריאה-בלבד.

---

# נספח ב' — סטטוס תיקונים (עודכן 2026-08-05, אחרי סבב התיקונים)

הדוח שמעל הוא **האבחון** ונשאר כפי שנכתב. זה מה שקרה מאז.

## תוקן ונאמת (15 קומיטים, לא נדחף)

| קומיט | ממצאים | מה נסגר |
|---|---|---|
| `269b1f6` | #5 | `var t` ב-streak.js — ה-XP היומי והמיילסטונים חזרו לעבוד |
| `1bdc3d2` | #3 | דחיית Promise ברינדור: באנר + טלמטריה במקום מסך לבן |
| `4284fbe` | #6 | סשן שלא הגיע לשרת נשמר בטלפון במקום להיעלם |
| `34ce8f8` | #4 | סשן אמיתי מזכה ב-XP, רצף ותגים (+טוסט, 7 שפות) |
| `019d6a7` | #2 | ה-Worker לא יכול להקפיא את הזיהוי — timeout + onerror + נפילה למסלול main-thread |
| `0da2eaf` | #7 | `claude-proxy` מוקשח — **נפרס 2026-08-05, גרסה 6 ACTIVE** |
| `267c609` | #8, #11 | `v10:auth` מגיע ל-window; `courtiq:session-saved` סוף-סוף נשלח |
| `b7ed09e` | #9 | תוכנית האימונים מסתנכרנת במפתח הנכון |
| `ffc1af1` | #12 | דגל ההגדרות פג אחרי מהלך אחד במקום להישאר לכל הסשן |
| `6464bb5` | #13 | `courtiq_onboarded` הוא מצב חשבון ומסתנכרן — ותיק לא נזרק לאונבורדינג |
| `2aa5170` | #15, #16 | שתי דליפות מאזינים (resize, keydown+DOM) |
| `85eca38` | #18 | `supabase-js` מקומי — אפס בקשות CDN באתחול |
| `e1dfbc9` | #17 | אוברליי ניתוח מפורק גם כשמישהו אחר מנווט |
| `17396cd` | #41, #42(חלקי) | כפתור "חזור" של אנדרואיד מחובר; `local-notifications` נרשם סוף-סוף לאנדרואיד |
| `2f0de01` | #10 | ה-XP במסך הבית כבר לא קפוא (max שרת/מקומי) |
| `e28fdba` | #20, #21, #22 | קריאות storage בלולאות רינדור: 42 אריחים → 2 קריאות; 500 תרגומים → 0 |
| `f4aaa2d` | #19, #25, #37, #38 | טיימר לפי שעון; דיבאג כבוי כברירת מחדל; מחיקת חשבון עם שגיאה; ביטול לניתוח |
| `d300fe6` | #26, #27, #28, #29, #45 | חמישה מרוצים ודליפות |
| `58b21e5` | #1, #30, #31, #32, #35, #36, #39, #40, #43 | ?studio, 78MB שנשלחו לווב, לינק עמוק, לולאת reload, נגישות |

**אימות סופי:** ‏`?smoke` = SMOKE PASS · 77 רינדורים (11 מסכים × 7 שפות): 0 שגיאות, 0 באנרים, 0 מפתחות גולמיים · כל 14 מחרוזות ה-i18n החדשות קיימות בכל 7 המשטחים.

## דורש אותך

1. **קריאה אחת מחשבון מחובר אמיתי ל-`claude-proxy`.** זה כל מה שנשאר, וזה כיסוי ולא סיכון — ראו למטה מה כבר אומת.
2. **מדידה על מכשיר (#14, `wasm-main-thread`).** עדיין הפריט היחיד שאי אפשר לסגור בלי אייפון אמיתי — פתח סשן עם `?debug=1` (שים לב: הדגל הפוך עכשיו) וצלם את ה-backend ואת ה-ms להיקש.
3. **‏#33/#34** — הגדרות בלוח הבקרה של Supabase, לא קוד.

### אימות `claude-proxy` (גרסה 6, ACTIVE) — מה נבדק ואיך

**חי, מול הפונקציה הפרוסה:**
* בלי טוקן → `401`. ‏JWT פגום → נדחה בשער.
* **המפתח האנונימי הציבורי כ-bearer → `401`.** זו הדלת האמיתית: המפתח הזה נמצא ב-`js/env.js` וכל אחד יכול לקרוא אותו מהאפליקציה. הוא עובר את שער ה-gateway (הוא JWT חתום) ונדחה על ידי הקוד שלנו, כי `getUser()` לא מחזיר משתמש.
* ‏`OPTIONS` → `200`, כלומר קריאות legacy לא נשברו.
* המקור הפרוס נקרא בחזרה — זהה בייט-לבייט לקומיט.

**הלוגיקה, מול המקור הפרוס עצמו** (הורץ ב-Deno עם import-map שמחליף רק את `supabase-js`; ‏`index.ts` לא שונה בתו):

| מקרה | תוצאה | הגיע ל-Anthropic? |
|---|---|---|
| מודל מחוץ לרשימה | `400` | **לא** |
| ‏`max_tokens: 100000` על מודל מותר | `200`, נשלח `3000` | כן, מוגבל |
| קריאת legacy רגילה (2400) | `200`, נשלח `2400`, ה-system נשמר | כן |
| ‏`stream: true` | `400` | **לא** |
| גוף מעל 60KB | `413` | **לא** |
| גוף שאינו JSON | `400` | **לא** |
| הקריאה ה-41 ביום (תקרה 40) | `429` | **לא** |
| ‏JWT לא תקין | `401` | **לא** |

**התלות של המגביל, מול המסד האמיתי:** ‏`coach_touch(uid uuid) returns integer`, ‏SECURITY DEFINER, מחזיר 1→2→3. זה היה סיכון ה-fail-open: אילו החתימה הייתה שונה, הקוד היה רושם אזהרה ו**מדלג** על המכסה. היא נכונה. (הרצתי על החשבון האמיתי ומחקתי את השורה — הטבלה חזרה לאפס רשומות להיום.)

**מה לא הורץ:** קריאת HTTP אחת של משתמש מחובר אמיתי דרך הפונקציה הפרוסה. לא ניתן להשיג JWT כזה בלי סיסמה או התחזות, ולא אעשה אף אחת מהן. כל מפרק בשרשרת אומת בנפרד — השער חי, הלוגיקה על הבייטים הפרוסים, וה-RPC מול המסד.

## תוקן אחרי הסבב הראשון

* **#44 — קבצי `js/` מתים (`0f96fc0`).** בפועל 29, לא 27: ‏`js/` החזיק 43 קבצים והאפליקציה טוענת 14. השאר — 490KB — נשלחו בכל בילד ונופרסו בכל אתחול. נמחקו אחרי שאומת שאין להם תג `<script>` באף HTML, אין גלובל ייחודי שקוד חי צורך, ואין טעינה דינמית.
* **הסיבה שהמחיקה לבדה לא הספיקה — וזה החצי החשוב:** ‏`build.js` העתיק ולא מחק, אז `www/js` המשיך להחזיק את כל 29 גם אחרי שנעלמו מהמקור. **מחיקת קובץ הייתה no-op מבחינת מה שנשלח בפועל.** עכשיו הספריות המועתקות ממורארות — כל מה שאין לו מקביל במקור נמחק. זה גם ניקה את שני קבצי ה-CSS היתומים שסעיף 4 מצא.
* אימות: ‏SMOKE PASS, ‏14 קבצי js נטענו עם 0 שגיאות 404, וסריקה של 12 מסכים × 7 שפות (84 רינדורים) בלי שגיאה, בלי באנר, ובלי אף אחד מ-14 הגלובלים שהקבצים המחוקים הגדירו.

## נשאר פתוח בכוונה

* **#23 — `document.write` לחבילות שפה.** הסינכרוניות כאן נושאת משקל: החבילה חייבת להירשם לפני שהמסכים רצים. המרה ל-`appendChild` משנה את סדר הטעינה, וזה סיכון רגרסיה גדול יותר מהרווח. לא נגעתי.
* **#24 — `updateProfile` קוד מת.** לחבר אותו זה פיצ'ר (כתיבת פרופיל לשרת), לא תיקון באג.
* **#42 — CI לאנדרואיד.** הרישום תוקן; הקמת CI היא עבודה נפרדת.
* **‏XP בין מכשירים.** ‏#10 תיקן את התצוגה; העברת XP בין מכשירים עדיין לא קיימת (מתועד ב-`lib/sync.js`).
